#!/bin/bash

# agent.jar downloaded to /home/vagrant/
# docker run -dit -v /home/vagrant/:/jenkins_slave -v /etc/localtime:/etc/localtime:ro --network host --name jenkin_slave java <Jenkin slave join script>
